manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
  projects:
    - name: zmk
      remote: zmkfirmware
      revision: v4.0  # Use a stable version instead of main
      import: app/west.yml
  self:
    path: config
